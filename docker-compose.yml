version: '3.8'

services:
  mysql:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: artbid
      MYSQL_USER: artbid
      MYSQL_PASSWORD: artbid
    ports:
      - "3306:3306"

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

  zookeeper:
    image: confluentinc/cp-zookeeper:7.6.0
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181

  kafka:
    image: confluentinc/cp-kafka:7.6.0
    depends_on:
      - zookeeper
    ports:
      - "9092:9092"
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://localhost:9092
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1

  minio:
    image: minio/minio
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: artbid
      MINIO_ROOT_PASSWORD: artbid1234
    ports:
      - "9000:9000"
      - "9001:9001"

  # 백엔드 코드가 준비되면 주석 해제해서 같이 띄우면 됩니다.
  # backend:
  #   build: ./backend
  #   depends_on:
  #     - mysql
  #     - redis
  #     - kafka
  #   ports:
  #     - "8080:8080"
  #   environment:
  #     SPRING_DATASOURCE_URL: jdbc:mysql://mysql:3306/artbid
  #     SPRING_DATA_REDIS_HOST: redis
  #     SPRING_KAFKA_BOOTSTRAP_SERVERS: kafka:9092
